---
type Props = {
  eyebrow?: string;
  title: string;
  text: string;
};

const { eyebrow, title, text } = Astro.props;
---

<section class="page-hero">
  <div class="container">
    {eyebrow && <span class="eyebrow">{eyebrow}</span>}
    <h1>{title}</h1>
    <p>{text}</p>
  </div>
</section>
